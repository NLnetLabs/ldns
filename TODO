Current TODO list

Implementation:
---------------
o deep free
o check all conversion routines on missing types
	- wire2host
	- host2wire
	- str2host
	- host2str
o network code
	- tsig?
o DNSSEC
	- signing
	- do we call random()?
o Make a shared library and test compile some programs
o Thread safety, strtok is used
o ssl autoconf
o create a bunch of small programs:
	dig
	host
	?
o SSL_load_error_str()? 
o resolver_frm_file (better parser)
o Remove all the printf's - and tie them into error handling
o per RR functions:
  - RRSIG
       ->keytag
       ->typecovered
   etc etc. For all types we have
o Remove/Handle all XXX and TODO/todos'
  checked:
  - rr.c/rr.h		- util.c
  - data.c/data.h	- dname.c
  - packet.c (partially)


Documentation:
--------------
o Crypto interface for dnssec
o Doxygen
o Manpages - can they be created or not?
o Introduction
o Tutorial
o Webpage
o Mailing list(s)
o Move all the documentation to the header files?
o Check the documentation

To ponder and discuss:
----------------------
o Second layer (higher level convenience functions)? (are already there)
o Dependency on openSSL?
o ldns_lookup_by_name works for types, classes
	should be shielded, only use ldns_get_class_by_name/type
o private key type?
	to many algs:
	ldns_algorithms[] host2str.c
	ldns_algorithm    dnssec.h
	ldns_signing_algorithm keys.h (new!?)
	
