Current TODO list

Roadmap:
1.0.x: stable releases, with only bugfixes
1.1.0: feature completeness + documentation
1.2.0: optimization


Post 1.0 Stuff:
---------------
        o Make drill a worthy dig replacement wrt to 
          DNSSEC debugging
        o Dname compression
        o A /fast/ impl. for holding a zone (and retrieving data from it)
        o refactor ldns_verify_rrsig_keylist and
          ldns_verify_rrsig (fold in TSIG stuff or leave it out?)
        o Different digest for DS (spec currently specifies SHA-1)
        o zone.c; make it better/faster??
        o SHA256 test in configure.ac
        o clean pkt->when stuff
        o edns extra rdata
        o printf like function with %-escapes to print ldns stuff

1.2.0:
	o More efficient zone and rr list structures
	o More (unit) tests
	o Split rr_lists from rr.[ch]
	o $INCLUDE (bindism).

DONE:
1.1.0 released 7 Juli 2006
1.1.0:
----
        o check sort order
        o secure tracing drill
	o API cleanup: add missing consts in function arguments
        o walker 
        o ldns_get_bla_frm_host doesn't parse aliases

1.0.1 released 5 January 2006
1.0.1:
------
        o Fix what needs to be fixed
